The Unix kernel, Unix shell, File system and basic shell commands. Unix utilities and shell programming. Unix system calls. Process control, signal handling and inter-process communication. The C programming language (control structures, functions, arrays, strings, pointers, dynamic memory management, low level operators and bit fields). Programming in Unix environment using the C language.
